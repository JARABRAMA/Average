package com.jarabrama.average.ui.theme.ui

import androidx.compose.foundation.layout.PaddingValues
import androidx.compose.ui.unit.dp

object Padding {
    val horizontal = PaddingValues(horizontal = 10.dp)
    val cardList = PaddingValues(vertical = 5.dp, horizontal = 10.dp)
    val littlePadding = PaddingValues(2.dp)
    val smallPadding = PaddingValues(5.dp)
    val normalPadding = PaddingValues(10.dp)
    val bigPadding = PaddingValues(20.dp)

}